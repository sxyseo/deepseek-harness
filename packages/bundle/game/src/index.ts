/**
 * @deepseek-ai/dsh-game — the game-engineering profile bundle. The package's
 * substance is `cordis.patch.yml`, declared by the `dsh.bundle.patch` manifest
 * field and resolved by the profile composer through that field: it mounts the
 * game runtime registry, the Godot backend, and the model-facing game tools
 * over dsh-base and reuses the dsh-headless one-shot runner; this module
 * carries no runtime API.
 * @module @deepseek-ai/dsh-game
 */

export {}
